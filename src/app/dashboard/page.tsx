import Navbar from '@/components/dashboard/Navbar';
import React from 'react';

const dashboard = () => {
    return (
        <div>
            <Navbar/>
            <h1>Dashboard</h1>
        </div>
    );
};

export default dashboard;